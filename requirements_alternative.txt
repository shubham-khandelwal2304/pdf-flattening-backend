# Alternative requirements for better cloud deployment
PyMuPDF==1.23.5
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
gunicorn==21.2.0

# If PyMuPDF still fails, try this alternative:
# pdf2image==1.16.3
# Pillow==10.0.1
# reportlab==4.0.4
